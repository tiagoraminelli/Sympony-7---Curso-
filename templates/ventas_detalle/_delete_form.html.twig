<form method="post" action="{{ path('app_ventas_detalle_delete', {'id': ventas_detalle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ventas_detalle.id) }}">
    <button class="btn">Delete</button>
</form>
